package main

import (
	"fmt"
	"strings"
)

func simplifyPath(path string) string {
	fields := strings.Fi
	stack := make([]string, 0, lenPath)
	for i := 0; i < lenPath; i++ {

	}
	return ""
}

func main() {
	fmt.Println(simplifyPath("/home/"))
}
